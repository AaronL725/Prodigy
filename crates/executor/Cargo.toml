[package]
name = "prodigy-executor"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
